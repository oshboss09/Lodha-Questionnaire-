{
  "entities": {
    "Question": {
      "title": "Question",
      "description": "An MCQ question",
      "type": "object",
      "properties": {
        "text": { "type": "string" },
        "options": { "type": "array", "items": { "type": "string" } },
        "correctAnswerIndex": { "type": "number" },
        "order": { "type": "number" }
      },
      "required": ["text", "options", "correctAnswerIndex"]
    },
    "Submission": {
      "title": "Submission",
      "description": "A completed test result",
      "type": "object",
      "properties": {
        "fullName": { "type": "string" },
        "department": { "type": "string" },
        "email": { "type": "string" },
        "score": { "type": "number" },
        "totalQuestions": { "type": "number" },
        "responses": { "type": "object" },
        "timestamp": { "type": "string", "format": "date-time" },
        "auto_submitted": { "type": "boolean" },
        "status": { "type": "string", "enum": ["complete", "incomplete", "Complete", "Incomplete"] }
      },
      "required": ["fullName", "department", "email", "score", "totalQuestions", "responses", "timestamp", "auto_submitted", "status"]
    },
    "Config": {
      "title": "Config",
      "description": "Global settings for the quiz",
      "type": "object",
      "properties": {
        "timerPerQuestion": { "type": "number" },
        "themePrimary": { "type": "string" },
        "googleSheetsWebhookUrl": { "type": "string" },
        "passThreshold": { "type": "number" },
        "excellentThreshold": { "type": "number" },
        "failTitle": { "type": "string" },
        "failDesc": { "type": "string" },
        "passTitle": { "type": "string" },
        "passDesc": { "type": "string" },
        "excellentTitle": { "type": "string" },
        "excellentDesc": { "type": "string" },
        "questionsPerAssessment": { "type": "number" }
      }
    },
    "EligibleParticipant": {
      "title": "EligibleParticipant",
      "description": "A participant allowed to take the assessment",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email", "createdAt"]
    }
  },
  "firestore": {
    "/questions/{questionId}": {
      "schema": "Question",
      "description": "Collection of all available questions"
    },
    "/submissions/{submissionId}": {
      "schema": "Submission",
      "description": "Participant test results"
    },
    "/config/global": {
      "schema": "Config",
      "description": "Global application configuration"
    },
    "/eligible_participants/{email}": {
      "schema": "EligibleParticipant",
      "description": "Collection of eligible participant email addresses"
    }
  }
}
